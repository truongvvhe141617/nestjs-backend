name: Test Windows Runner

on:
  push:
    branches:
      - master
      - main

jobs:
  test:
    runs-on: [self-hosted, Windows]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Show system info
        run: |
          node -v
          npm -v
          echo %CD%
